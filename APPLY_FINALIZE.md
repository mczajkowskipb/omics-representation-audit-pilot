# Apply final Pilot v2 evidence packaging

From the repository root, unpack this ZIP, then run:

```bash
PYTHON_BIN=.venv/bin/python bash scripts/22_finalize_pilot_v2.sh
```

Review `docs/pilot_v2/PILOT_EVIDENCE.md` and `git status --short`, then commit the tracked Pilot v2 code, protocol, evidence snapshots and figures on `pilot-v2-relational-prototypes`.
